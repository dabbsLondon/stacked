# Stacked — PocketBase backend URL.
#
# When this is set, the app gates behind sign-in and syncs projects to the
# PocketBase instance at this URL. Leave unset (or delete the file) to run
# in pure-localStorage mode without authentication.
#
# Local development default:
VITE_PB_URL=http://127.0.0.1:8090
