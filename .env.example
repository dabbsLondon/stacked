# Stacked — Supabase configuration. Copy to `.env.local` and fill in the
# real values from your Supabase project's Settings → API.
#
# Leave these unset (or delete the file) to run Stacked in pure-localStorage
# mode without authentication.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
